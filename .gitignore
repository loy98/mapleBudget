node_modules
dist
dev-dist
.DS_Store

# 환경변수 (Supabase URL/anon key 등) — 로컬 전용
.env.local
.env*.local

# Supabase CLI 로컬 캐시(연결 프로젝트 ref·서비스 버전 등) — 기기별, 커밋 금지
supabase/.temp/

# Dev scaffolding / orchestration state
.omc
_archive

# 디자인시스템 동기화 스크래치(로컬 전용)
.design-sync/

# Claude Code 로컬 권한 오버라이드(개인 전용)
.claude/settings.local.json

# Claude Code IDE 실행 설정(기기별) — 저장소에 넣지 않는다
.claude/launch.json
